# Flutter/Dart build output — regenerated by `flutter pub get` and the
# native shells created by bootstrap.sh.
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
.packages

# Native shells are generated locally by bootstrap.sh.
/android/
/ios/
/linux/
/macos/
/windows/
/web/
